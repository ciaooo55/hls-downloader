---
repository: ciaooo55/hls-downloader
default_branch: main
product_version: 7.0.2
release_ready: false
last_updated: 2026-09-11
---

# Project handoff

## Current baseline

- The canonical product state is `artifacts/v7-productization/feature-parity.json`: 27 verified, 1 partial, 0 blocked, `release_ready=false`.
- The remaining partial feature is `browser.media_push_device_selection`; its source path is implemented, but final acceptance still requires the installed-package real-browser and real-LAN-device gate.
- The active implementation is only `desktop_ui/`, `native_shell/`, `presenter_ui/`, and `extension/`.
- `native_shell` is the sole resident Core and SQLite owner. Compose and Presenter communicate through `hls-downloader-v7-core` on `\\.\pipe\HLSDownloader.v7`.
- `main` remains the integration baseline and must not be modified or merged from this workstream without explicit operator authorization.

## `网页版gpt` workstream

- This continuation workstream is branch-only: assistant-authored changes stay on `网页版gpt`; any integration observed on `main` is external unless explicitly authorized.
- PR #92 was merged outside this workstream at `705b532c9084f7f2cb8c0a24c1bf4c7f8206d8c2`, initially advancing `main` through that docs-only handoff head. Post-merge continuation uses Draft PR #93 only to restore pull-request CI/Security/Candidate validation for `网页版gpt`; do not merge PR #93 and do not write `main` from this workstream.
- Subsequent external activity advanced `main` again to `e1db63e13c9485eaa64d60d5367cdb4cca97fd13` without a write from this workstream. The later product-source commits `4559b88c23f0b051b9eb5f9fb5d35956d42c4c25`, `4e6f8d1f89272d9977d0860cb185423bd0ba7830`, `a102ce007ae08a176a0018e23d3236107f07e630`, `73985894d919df38869fb5b39dae55db53c6346d`, `97c0314cff89f92d14b0fc708f57ff00d9129291`, and `4146a0f7e7a7ca46eedcfa60fe5ede6cd6bd88ac` remain branch-only behind Draft PR #93, followed only by handoff documentation commits.
- Recent hardening makes cURL long options with inline values (`--option=value`) follow the same validation path as split arguments, including proxy, cookie-file, user and data-urlencode rejection rules.
- The reviewed Metalink XML entity fix is integrated on this branch: predefined and numeric XML entities are decoded before remote-URL safety checks, preserving signed query strings such as `&amp;`.
- Proxy route identity hardening is complete in `e38bba5e7adb05404e36e3fa7f9cf75021354c6e`: direct/system/manual identities survive task/site/global policy resolution; site-rule `system` overrides global routes; bypass remains forced-direct; system routing uses WinHTTP automatic proxy selection; curl-impersonate does not silently bypass system routing; direct curl fallback disables environment proxying.
- HTTPS/custom POST body forwarding is complete in `9b6dae814d59d0b3a128084cc5dac18949c72b2d`: WinHTTP sends the persisted request body and curl fallback preserves POST/HEAD method/body semantics without inventing Content-Type.
- Duplicate reuse request identity hardening is complete in `c1d60cce6c7534130fd7021fff352a1a879e4963`: URL-based reuse is limited to safe comparable requests and requires matching method plus normalized non-sensitive headers; credential-backed, sensitive-header and POST/body requests do not reuse stale tasks.
- FTP/FTPS stalled data-channel task control hardening is complete in `e3d843d6039111af257cb172ba093396a7eeafde`: blocking data reads no longer prevent prompt pause/cancel, including Schannel-backed FTPS.
- Native Host replay-credential rollback is complete in `73af496086029d77ae9a5c85b7a719d995e2104f`: resident Core IPC exposes credential deletion and failed/no-task browser CreateTask rolls back only the replay credential created by that request.
- Selected external HLS AUDIO result integrity is complete in `8d549b4c396d9a95d81357ef5446feb768e8264e`: VOD propagates selected-audio download failure, Live propagates worker errors and join panic, while subtitle best-effort behavior remains unchanged. Focused Windows validation plus the full HLS test group passed. The same product source then passed full v7 CI #738 and Maintenance Security #233 on connector-authored head `8e2a2a0344caf80c87096a9eea01e3a21acbab92`.
- Legacy 5.x settings migration atomicity is complete in `450046133d52289f9ee0ae8c1a684b65e5488b5f`: legacy ordinary settings are collected first and committed through the existing transactional `CoreStore::set_settings()` boundary; default-cookie decode/protect failure happens before settings writes; when a cookie update precedes a settings transaction failure, the previous credential is restored or the newly-created credential is deleted. Windows format/check, the failure-atomicity regression, normal cookie/settings import regression, and the full migrate test group passed.
- FTP URL percent decoding is fail-closed in `bf3d5a2bf2403a78621ba58ae5e0cf4be62b112b`: incomplete percent escapes, invalid hex and invalid decoded UTF-8 are rejected instead of being passed literally or replaced lossily. Existing UTF-8 user/password/path decoding and stalled-transfer pause/cancel coverage remain green in the full FTP test group.
- Media URL static recognition is tightened in `038f8b3a62c0d2414bb14a60733d0e4a194544f9`: `.m3u8` and `.mpd` classification requires a real path suffix rather than a substring such as `.m3u8.txt`, and `live=false/no/off` is case-insensitive. Content-based HTTP probing remains unchanged and can still identify opaque HLS/DASH endpoints. Format/check and all recognition tests passed.
- HLS live-name recognition false positives are closed in `b1a8fbc251fd373b1a221c04d39c8c9ea1c5c0db`: static/content-sniffed HLS classification now tests `live` only as a token in the actual URL path, so hostnames such as `live.example.com` and unrelated path components such as `deliver`/`olive` no longer force `ResourceKind::Live`; `/live/`, `live.m3u8`, `my-live-stream.m3u8`, and explicit `live=` query semantics remain covered by regressions.
- Opaque HTTP content probing now treats the `http`/`https` scheme case-insensitively in `0b8448c478833fb592b787f2ddb9052a3a3e45be`, matching the transport/safety layer. Uppercase forms such as `HTTPS://host/opaque` therefore retain HLS/DASH/page content sniffing instead of being skipped. The exact source head passed v7 CI #748 Rust Core full test/build/lint, Native presenter, Browser extensions, and contract validation before branch-only validation cleanup; the remaining Compose/package jobs are independent of these recognition changes.
- HTML harvest URL resolution preserves repeated path separators in `7ddb4e4f9a297104d9035f885bf2a724c9a50fba`: dot-segment cleanup still resolves `.` and `..`, but valid repeated separators such as `/cdn//signed/file.zip` are no longer collapsed into a different resource path. Regressions cover absolute and relative signed-style paths plus trailing-directory semantics.
- HTML harvest entity handling is hardened in `89b3848782ee78c6a96c4e4279a198a6a9ebb7eb` with the rustfmt-only follow-up `8c1200ebe5250d68e5a1e0b7d2a470ff44330d96`: standard decimal/hex numeric entities such as `&#38;` and `&#x26;` are decoded exactly once, named entities remain supported, and a decoded leading BOM is stripped before scheme validation so encoded unsafe schemes cannot fall through as relative paths. Exact source head `8c1200e` passed full v7 CI #753, including Rust Core test/build/lint, Native presenter, Compose workbench/distribution, Browser extensions and contracts, and Maintenance Security #248 also passed. The preceding #752 failure stopped at `cargo fmt --check` before tests and was resolved only by the formatting follow-up; no logic change was required.
- Browser `Content-Disposition` filename fallback is hardened in `c49752cce64fde0a5b317bcd8005b57eb9eb66d4`: RFC 5987 `filename*` still has priority when it produces a usable cleaned name, but an extended value that decodes only to stripped control bytes no longer suppresses a valid legacy `filename`. The regression covers the unusable-extended/valid-legacy case. Exact source head `c49752c` passed full v7 CI #755 and Maintenance Security #250.
- Browser `Content-Disposition` extended-name charset handling is fail-closed in `377b2a148f1166b62ef7883fc37082fb859b2867`: a `filename*` that declares an unsupported charset is no longer silently decoded as UTF-8 and allowed to suppress a valid legacy `filename`; unsupported encodings now fall back to the legacy name. The regression covers an unsupported UTF-16 declaration. Exact source head `377b2a1` passed full v7 CI #763, including Rust Core test/build/lint, Native presenter, Compose workbench/distribution, Browser extensions, contracts and aggregate gate, and Maintenance Security #258 also passed.
- Browser `Content-Disposition` extended-name byte decoding is fail-closed in `8fbaa66f5945363286923277ed884f0904aec5c9`: supported charsets now decode with fatal error handling, so malformed byte sequences such as an invalid UTF-8 `%FF` do not produce replacement-character or raw-percent garbage that suppresses a valid legacy filename. The regression requires fallback to the legacy name on invalid encoded bytes. Exact source head `8fbaa66` passed full v7 CI #764, including Rust Core test/build/lint, Native presenter, Compose workbench/distribution, Browser extensions, contracts and aggregate gate, and Maintenance Security #259 also passed. Candidate Package #413 was still building when this handoff was updated, so no candidate artifact or package success is claimed from that run.
- Browser POST form-data replay exactness is hardened in `a082fabdbeb4606cdadef532ad555e1f35c075ab`: request capture no longer slices excess form fields or values and then replays a semantically changed POST. Bodies beyond the bounded 128-field or 128-values-per-field capture policy are rejected for replay instead; small bodies retain their full ordered URL-encoded representation. Regressions cover normal reconstruction, excess fields and excess values. Exact source head `a082fab` passed full v7 CI #756, including Rust Core test/build/lint, Native presenter, Compose workbench/distribution, Browser extensions and contracts, and Maintenance Security #251 also passed.
- Early Chromium direct-response takeover is fail-closed in `81daf9aa4d5c46e6515e42608b97e9cea3a8b65c`: the optional response status must now be a finite 2xx code before an attachment/media navigation can be pre-offered. Missing or `NaN` status no longer falls through the numeric comparisons as if it were successful. Regressions cover both statusless and non-finite responses. Exact source head `81daf9a` passed full v7 CI #758 and Maintenance Security #253.
- DASH URL entity handling is hardened in `969576a584a823e25f15bdeca359ab642426d6ad`: decimal and hexadecimal numeric XML character references are decoded before resolving `BaseURL` and `SegmentTemplate` playback URLs, so signed queries such as `&#38;` / `&#x26;` and encoded separators such as `&#47;` are not treated as literal URL text. Invalid Unicode scalar values remain unchanged instead of being synthesized. A regression covers numeric entities in both the base URL and initialization/media templates. Exact source head `969576a` passed full v7 CI #759, including Rust Core test/build/lint, Native presenter, Compose workbench/distribution, Browser extensions and contracts, and Maintenance Security #254 also passed.
- DASH XML entity decoding is exact-once in `5c2614fa954b611b8f56e50f6bdf6aa13a6cdd37`: named and numeric references are decoded in one source-text pass, so a source sequence such as `&#38;amp;` becomes the literal `&amp;` rather than being recursively decoded into `&`. The regression covers both initialization and media-template paths. Exact source head `5c2614f` passed full v7 CI #761, including Rust Core test/build/lint, Native presenter, Compose workbench/distribution, Browser extensions, contracts and aggregate gate, and Maintenance Security #256 also passed. Candidate Package #410 was still building when this handoff was updated, so no candidate artifact or package success is claimed from that run.
- Static DASH multi-Period duration handling is corrected in `e1772ab86f3dca376deb43b17deac9e224ddebe1`: root `mediaPresentationDuration` remains authoritative when present; otherwise the browser inspection now sums Period durations only when every Period has an explicit finite duration, instead of using only the first Period and underestimating total duration/estimated size. If any Period duration is missing, the parser leaves duration and estimated size unknown rather than inventing a partial total. Regressions cover both complete multi-Period summation and the fail-closed partial-duration case. Exact source head `e1772ab` passed full v7 CI #766, including Rust Core test/build/lint, Native presenter, Compose workbench/distribution, Browser extensions, contracts and aggregate gate, and Maintenance Security #261 also passed. Candidate Package #415 was still building when this handoff was updated, so no candidate artifact or package success is claimed from that run.
- DASH explicit zero-duration handling is corrected in `0314c698e75e4b8c7c30f6d11d70f48461d1b8ba`: ISO duration parsing now distinguishes invalid or absent values from a valid zero value, so `PT0S` can participate in complete multi-Period summation without being mistaken for a missing duration; empty-component forms such as bare `P`/`PT` remain invalid. A regression covers `PT0S + PT10S => PT10S`. Exact source head `0314c69` passed full v7 CI #768, including Rust Core test/build/lint, Native presenter, Compose workbench/distribution, Browser extensions, contracts and aggregate gate, and Maintenance Security #263 also passed. Candidate Package #417 was still building when this handoff was updated, so no candidate artifact or package success is claimed from that run.
- Browser manifest text decoding is fail-closed in `1830ade7028aee9a2be66622798ecfd9b9ee5796`: the bounded HLS/DASH response reader now uses fatal streaming UTF-8 decoding, preserving valid multibyte characters across chunk boundaries while rejecting malformed bytes or truncated code points instead of synthesizing replacement characters that can mutate manifest URLs. Regressions cover a split multibyte character, immediate invalid UTF-8 with stream cancellation, and an incomplete terminal sequence. Exact source head `1830ade` passed full v7 CI #770, including Rust Core test/build/lint, Native presenter, Compose workbench/distribution, Browser extensions, contracts and aggregate gate, and Maintenance Security #265 also passed. Candidate Package #419 was still building when this handoff was updated, so no candidate artifact or package success is claimed from that run.
- HLS segment-duration metadata is fail-closed in `2976e2865fe185e3ab99bd9526158d82e477de01`: any `EXTINF` duration that is non-finite or negative now makes aggregate duration/estimated size unknown instead of allowing `Infinity` or a backwards duration adjustment to poison VOD metadata; a valid zero-length segment remains compatible with later finite segments. Regressions cover infinite, negative and zero-plus-valid durations. Exact source head `2976e28` passed full v7 CI #772, including Rust Core test/build/lint, Native presenter, Compose workbench/distribution, Browser extensions, contracts and aggregate gate, and Maintenance Security #267 also passed. Candidate Package #421 was superseded by the subsequent adaptive-number source hardening, so no candidate artifact is claimed from that run.
- Adaptive manifest numeric metadata is bounded in `2cd78d730945a49b2565de8b1daef198910a8c8f`: HLS variant bandwidth/resolution and DASH representation bandwidth/width/height accept only finite positive values, while overflowing DASH ISO durations are treated as unknown. Malformed values therefore cannot surface as `Infinityp`, negative/non-finite bandwidth or infinite estimated size. Regressions cover HLS bandwidth/resolution overflow/negative input and DASH overflowing duration plus non-finite/negative representation metadata. Exact source head `2cd78d7` passed full v7 CI #773, including Rust Core test/build/lint, Native presenter, Compose workbench/distribution, Browser extensions, contracts and aggregate gate, and Maintenance Security #268 also passed. Candidate Package #422 was still building when this handoff was updated, so no candidate artifact or package success is claimed from that run.
- DASH explicit Period duration aggregation is fail-closed in `0b71e0741d63fd6858cbecfee85c9162dbbde81e`: after every Period duration individually parses as finite, the aggregate sum must also remain finite; a multi-Period sum that overflows is treated as unknown instead of leaking `Infinity` into total duration or estimated size. A regression covers two individually finite Period durations whose sum overflows. Exact source head `0b71e07` passed full v7 CI #775, including Rust Core test/build/lint, Native presenter, Compose workbench/distribution, Browser extensions, contracts and aggregate gate, and Maintenance Security #270 also passed. Candidate Package #424 was superseded by the subsequent adaptive-size arithmetic hardening, so no candidate artifact is claimed from that run.
- Adaptive estimated-size arithmetic is fail-closed in `a04328463b3f8bbb5b08432fc85b0d90f9765d3e`: HLS and DASH no longer expose `Infinity` when individually finite duration/bandwidth values overflow during multiplication, DASH finite video/audio bandwidth addition must remain finite before size estimation, and max-audio selection no longer relies on array spreading. HLS also rejects a non-finite/negative captured fallback estimated size. Regressions cover HLS multiplication overflow plus DASH multiplication and combined-bandwidth overflow. Exact source head `a043284` passed full v7 CI #776, including Rust Core test/build/lint, Native presenter, Compose workbench/distribution, Browser extensions, contracts and aggregate gate, and Maintenance Security #271 also passed. Candidate Package #425 was still building when this handoff was updated, so no candidate artifact or package success is claimed from that run.
- HLS entry-to-URI ownership is bounded in `e1db63e13c9485eaa64d60d5367cdb4cca97fd13`: a malformed `EXT-X-STREAM-INF` without its own URI no longer borrows the next variant URI, and an `EXTINF` without its own segment URI no longer borrows a later segment while still permitting intervening segment tags such as `EXT-X-BYTERANGE`. Regressions cover missing variant URI, missing segment URI, and BYTERANGE compatibility. Exact source head `e1db63e` passed full v7 CI #779, including Rust Core test/build/lint, Native presenter, Compose workbench/distribution, Browser extensions, contracts and aggregate gate, and Maintenance Security #274 also passed. Candidate Package #428 was superseded by subsequent HLS hardening, so no candidate artifact is claimed from that run.
- HLS aggregate segment duration is fail-closed in `4559b88c23f0b051b9eb5f9fb5d35956d42c4c25`: even when every `EXTINF` value is individually finite and non-negative, the accumulated total must remain finite; aggregate overflow now makes duration/estimated size unknown instead of leaking `Infinity`. The regression covers two individually finite segment durations whose sum overflows while preserving their playback URLs. Exact source head `4559b88` passed full v7 CI #780, including Rust Core test/build/lint, Native presenter, Compose workbench/distribution, Browser extensions, contracts and aggregate gate, and Maintenance Security #275 also passed. Candidate Package #429 was superseded by the subsequent malformed-variant hardening, so no candidate artifact is claimed from that run.
- Malformed HLS variant URLs are isolated in `4e6f8d1f89272d9977d0860cb185423bd0ba7830`: an invalid `EXT-X-STREAM-INF` URI now drops only that malformed variant instead of throwing out the entire master inspection, allowing later valid variants to remain available. The regression covers a malformed absolute variant URL followed by a valid 1080p variant. Exact source head `4e6f8d1` passed full v7 CI #782, including Rust Core test/build/lint, Native presenter, Compose workbench/distribution, Browser extensions, contracts and aggregate gate, and Maintenance Security #277 also passed. Candidate Package #431 was superseded by the subsequent handoff documentation update and cancelled, so no candidate artifact is claimed from that run.
- HLS selected-rendition enrichment is best-effort in `a102ce007ae08a176a0018e23d3236107f07e630`: once a master playlist has parsed successfully, a network exception while fetching the selected child rendition no longer discards the already reliable master variants, resolution and bandwidth metadata; primary master-fetch failure semantics are unchanged. The regression covers a successful master followed by a throwing child fetch and requires the parsed 1080p variant metadata to remain available. Exact source head `a102ce0` passed full v7 CI #785, including Rust Core test/build/lint, Native presenter, Compose workbench/distribution, Browser extensions, contracts and aggregate gate, and Maintenance Security #280 also passed. Candidate Package #434 was superseded by the subsequent handoff documentation update and cancelled, so no candidate artifact is claimed from that run.
- Extensionless DASH content sniffing accepts a leading XML declaration in `73985894d919df38869fb5b39dae55db53c6346d`: after BOM/leading-whitespace normalization, `detectManifestKind()` now skips only an opening `<?xml ...?>` declaration before checking the MPD root, so standard XML-declared MPDs served from manifest-like extensionless URLs or generic MIME types are no longer missed; HLS and ordinary HTML behavior remain unchanged. The regression covers BOM + whitespace + XML declaration + MPD. Exact source head `7398589` passed full v7 CI #787, including Rust Core test/build/lint, Native presenter, Compose workbench/distribution, Browser extensions, contracts and aggregate gate, and Maintenance Security #282 also passed. Candidate Package #436 was superseded by the subsequent namespaced-DASH source hardening, so no candidate artifact is claimed from that run.
- Namespaced DASH element parsing is aligned with sniffing in `97c0314cff89f92d14b0fc708f57ff00d9129291`: the lightweight browser parser now accepts optional XML namespace prefixes on the supported MPD, Period, AdaptationSet, Representation, BaseURL and SegmentTemplate elements, so a manifest recognized as `<mpd:MPD>` is no longer discarded during inspection. The regression covers a fully `mpd:`-prefixed static MPD and verifies duration, 1080p metadata, bandwidth, estimated size, nested BaseURL and SegmentTemplate playback hints. Exact source head `97c0314` passed full v7 CI #789, including Rust Core test/build/lint, Native presenter, Compose workbench/distribution, Browser extensions, contracts and aggregate gate, and Maintenance Security #284 also passed. Candidate Package #438 was superseded by the subsequent HLS duration source hardening, so no candidate artifact is claimed from that run.
- Empty HLS segment durations are fail-closed in `4146a0f7e7a7ca46eedcfa60fe5ede6cd6bd88ac`: an empty or whitespace-only `EXTINF` duration is no longer coerced by JavaScript into a valid zero-second segment, preventing a malformed entry from silently underestimating the aggregate VOD duration when later finite segments exist. Explicit `EXTINF:0` remains valid. The regression covers an empty duration followed by a five-second segment while retaining both playback URLs. Exact source head `4146a0f` passed full v7 CI #790, including Rust Core test/build/lint, Native presenter, Compose workbench/distribution, Browser extensions, contracts and aggregate gate, and Maintenance Security #285 also passed. Candidate Package #439 was still building when this handoff was updated, so no candidate artifact or package success is claimed from that run.
- The temporary recognition validation workflow was removed in `d1a31603afaae8b1326005a64418668d48f6a1bd`; focused validation helpers are not product surface.
- Temporary repair workflows/scripts used for focused Windows validation are removed after tested source commits land; they are not product surface.
- Candidate Package #383 on pre-HLS source head `d51a1262d1f1d9ef9db1decd801c4f3d11039685` completed successfully with artifact `10188409926`, size `809272400`, digest `sha256:b365c7083475739610c75f469b9cb6a6989cda1244228ccb7e9c1325f395b0a0`.
- Candidate #387 on the HLS-validated connector head was intentionally superseded by the subsequent migration/FTP/recognition source-hardening commits; its cancellation is not a product failure. Subsequent recognition, harvest and extension hardening remains isolated to this branch and does not change the release-readiness boundary.
- No further reviewed source defect is queued for automatic modification. Authenticated manifest-inspection credential policy, malformed extended-filename percent-escape compatibility, UNC network-share behavior, and task-export replay fidelity remain explicit product/compatibility decisions rather than automatic hardening targets.
- The remaining product release gate is `browser.media_push_device_selection`. Existing source verification covers Native Host request IDs, Core persistence/resolution, Compose requested/resolved handling, completion feedback, extension polling, and a strict real-device smoke path. Do not mark it verified until the installed candidate is exercised through real browser registration, the Compose device picker, and a real LAN/TVBox receiver.

## Repository guidance

- Treat `main` plus the canonical feature matrix as the release source of truth; branch-specific hardening remains isolated until explicitly authorized for integration.
- Files under `docs/coordination/`, `docs/worker-logs/`, and `docs/manger.log` are retained historical execution evidence, not live task-assignment instructions.
- Historical Python/FastAPI, React/Tauri, WebView2, and v6 implementations remain available through Git history and tags; do not restore them as active directories.
- Candidate CI success does not authorize formal publication. Formal packaging still requires complete canonical parity, a separately reviewed `release_ready=true`, exact-main evidence, visual/performance/installer/rollback gates, signing, and explicit operator authorization.

## Validation

Repository validation commands remain defined by `AGENTS.md`. Keep `release_ready=false` until the real installed-browser/LAN media-push gate is accepted and canonical metadata is separately reviewed.
