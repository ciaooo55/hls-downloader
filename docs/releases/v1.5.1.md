# HLS Downloader v1.5.1

## TVBox 设置体验

- 扫描到的设备和手动填写的地址现在是明确的两种选择，不会再出现已填写地址但下拉框显示“不使用”的歧义。
- 手动地址支持填写带 `/action` 的 TVBox 接口；保存时由桌面端统一校验和规范化。
- 设置弹窗支持完整键盘操作：打开后焦点进入弹窗、Tab 不会逸出、Esc 可按当前上下文关闭，并为选项卡补齐语义。

## 界面一致性与性能

- 工具栏和侧栏统一使用单一主色，移除无意义的渐变，保持下载管理器的清晰层级。
- 任务进度条改为仅动画 transform，降低频繁刷新时的布局压力。
