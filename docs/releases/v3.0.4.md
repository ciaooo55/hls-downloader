# HLS Downloader v3.0.4

- 修复 HLS VOD 在部分按单连接限速的 CDN 上只能跑到约 1 MB/s 的回归。
- HLS 分片并发恢复为独立 HTTP/1.1 连接；并发数再次能真实叠加带宽。
- 浏览器捕获的 Cookie、Referer、Origin、User-Agent、TLS 指纹、边下边播、断点和合并流程保持不变。
- 桌面端、Core、Native Host、Chromium 与两个 Firefox 变体统一为 3.0.4。
