# HLS Downloader v3.0.10

这是桌面端状态显示修复版本，浏览器插件仍为 v3.0.9，无需重新安装。

- 修复多个浏览器或多个插件版本连接过后，当前 v3.0.9 插件被错误提示“需要升级到 v3.0.9”的问题。
- 活跃插件存在时，只根据仍在连接的客户端决定侧边栏升级警告；已断开的历史客户端继续保留在详细列表中用于排查。
- 同时连接新旧插件时，提示会明确显示具体浏览器和旧插件版本，不再把最高版本显示为待升级版本。
