# HLS Downloader v1.6.19

- HLS 主清单包含独立音轨时，不再直接标为不支持。下载器会自动转用内置自适应兼容引擎，下载并合并最佳视频与对应音频轨。
- 兼容引擎继续使用浏览器捕获的 Cookie、Referer、Origin 和请求头，适用于需要登录或短期签名的 HLS 页面。
- 状态提示会明确显示“HLS 独立音轨”，避免将这类 HLS 兼容下载误显示为 DASH。
