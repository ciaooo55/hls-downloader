# HLS Downloader v1.4.4

## 完成收口

- App 壳层状态接入 Zustand（筛选、搜索、主题），与设置页主题同步更稳定。
- 设置页、任务详情、任务状态标签进一步迁到统一 UI 原语（Button / Dialog / Badge）。
- 队列说明补全：设置中的同时任务数/定时队列提示与右键优先级操作对齐。
- Dialog 支持正确的 role 覆盖（确认框 alertdialog）。
- 补充 uiStore 单测，回归队列与 403 诊断行为。

## 验证

- Python 全量回归。
- 前端 vitest + 生产构建。
- Windows Release 工作流产物。
