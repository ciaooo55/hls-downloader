# HLS Downloader v5.0.6

原生任务列表：全部 / 未完成 / 已完成，加上视频、音乐、压缩包、文档、程序、常规筛选，以及搜索、开始/暂停/删除、打开目录。关闭列表回到托盘，下载继续。设置和新建仍打开现有桌面窗口。

托盘「打开」在监督进程已热时显示这个原生列表，不再先拉一层任务 WebView。确认/进度/完成小窗和 HTTP Range 单文件写入保持 5.0.5 的行为。

从 GitHub Release 安装 `HLSDownloader-v5.0.6-Windows-x64-Setup.exe`（或 Portable.zip）。
