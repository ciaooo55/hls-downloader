# HLS Downloader v1.6.13

- 浏览器插件现在能识别 JavaScript 下载链接和 `<a data-download-url>` 一类常见站点控件；只有存在实际下载地址、下载语义或用户 Ctrl 强制时才会记录接管意图，普通导航不受影响。
- 保留 v1.6.12 的 `Retry-After` 退避、v1.6.11 的安全断点续传，以及现有 HLS/DASH/BT/普通文件能力。
