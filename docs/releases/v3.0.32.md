# HLS Downloader v3.0.32

浏览器普通下载接管按现有规则补两处会把本该接下的文件留给浏览器的缺口。图片、脚本、登录页和未标附件的动态页面仍不接管。

- 服务器标了 `Content-Disposition: attachment` 的二进制/PHP 下载，即使没有文件扩展名也会接管；和 JSON 附件同一条规则。
- 磁链点击进入接管判定时不再因为协议不是 http 被直接丢掉。
