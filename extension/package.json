{
  "name": "hls-downloader-extension",
  "private": true,
  "version": "7.0.0",
  "type": "module",
  "scripts": {
    "build:chrome": "wxt build -b chrome",
    "build:firefox": "wxt build -b firefox",
    "build": "wxt build -b chrome && wxt build -b firefox",
    "zip:chrome": "wxt zip -b chrome",
    "zip:firefox": "wxt zip -b firefox",
    "zip:firefox-store": "wxt zip -b firefox --sources",
    "package:store": "pnpm run build && pnpm run zip:chrome && pnpm run zip:firefox-store",
    "typecheck": "wxt prepare && tsc --noEmit",
    "test": "wxt prepare && tsc --noEmit && vitest run"
  },
  "devDependencies": {
    "@types/chrome": "0.0.326",
    "typescript": "5.8.3",
    "vitest": "3.2.4",
    "web-ext": "10.5.0",
    "wxt": "0.20.27"
  },
  "packageManager": "pnpm@11.7.0"
}
