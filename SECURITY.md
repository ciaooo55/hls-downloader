# 安全政策

## 支持范围

安全修复优先覆盖当前 GitHub Release 与 `main` 分支。旧版本发现问题时，请先确认最新版是否仍可复现。

## 私下报告漏洞

涉及浏览器扩展、本地 API、Native Messaging、更新器、任意文件访问或凭据泄露的问题，请使用 GitHub 仓库的 **Security → Report a vulnerability** 私下报告，不要先创建公开 Issue。

报告请包含：受影响版本、复现步骤、预期与实际结果、影响范围，以及已脱敏的日志。请勿提交真实 Cookie、Token、Authorization、签名 URL、个人文件或下载内容。

维护者会先确认收到报告，再评估严重性、准备修复并协调披露。未经授权请勿测试不属于你的账户、设备或网络。

## 安全边界

本项目不绕过 DRM、EME 或 SAMPLE-AES，也不接受要求规避访问控制的功能请求。浏览器扩展只应转交浏览器实际观察且用户有权下载的资源。
